<%= form_with(model: conversation, local: true) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
      <% conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id, id: :conversation_owner_id %>
  </div>

  <div class="field">
    <%= form.label :hirer_id %>
    <%= form.text_field :hirer_id, id: :conversation_hirer_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
