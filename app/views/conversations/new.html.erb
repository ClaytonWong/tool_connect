<%= link_to 'Back to listing', tool_path(params[:tool_id]) %>

<%= form_with(model: @conversation, local: true) do |form| %>
  <% if @conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
      <% @conversation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.hidden_field :owner_id, value: params[:owner_id] %>
  <%= form.hidden_field :hirer_id, value: params[:hirer_id] %>
  <%= form.hidden_field :tool_id, value: params[:tool_id] %>
  <div class="actions">
    <%= form.submit "create or continue with conversation" %>
  </div>
<% end %>
<!--<h1>New Conversation</h1>-->

<!--<%#= render 'form', conversation: @conversation %>-->

<!--<%#= link_to 'Back', conversations_path %>-->
